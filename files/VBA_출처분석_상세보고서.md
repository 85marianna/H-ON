 # H-ON 회계 시스템 VBA 모듈 출처 분석 보고서

**분석 대상**: `H-On accounting system_v0.9.xlsm` (36개 모듈)  
**비교 기준**: 오빠두엑셀 "재고관리 프로그램 5일차 - 예제파일.xlsm" (16개 모듈)  
**분석 방식**: 코드 비교 + 함수명 + 변수명 + 도메인 로직 분석  
**작성일**: 2026-08-09

> **핵심**: 코드 수정 없이 분석만 수행했습니다. 모든 파일의 원본 상태를 유지했습니다.

---

## 📊 전체 분류 요약

총 36개 모듈 분류 결과:

| 분류 | 개수 | 모듈 |
|------|------|------|
| 🔴 오빠두 잔재 가능성 높음 | 3개 | Mod_WS, frmProduct, frmProductSelect |
| 🟠 오빠두 기반 + H-ON 수정 | 8개 | z_Mod_DB, z_Mod_Array, z_Mod_Frm, frmCalendar, z_Mod_Sort, z_Module_GetCursor, z_Mod_Shape, z_Module_ListWheel |
| 🟡 혼합/판단 필요 | 0개 | - |
| 🟢 H-ON 자체 코드 | 24개 | HJ_*, Mod_ACCESS*, frm거래처*, frm반제*, frm전표*, frm외화*, frm원장*, frm송금*, frm제예금*, frm채권*, frm즐겨*, frm보고서*, frm로그인*, frmMain, frmAcctInput, frm결산마감, frm계정검색 |
| ⚪ 출처 판단 불가 | 1개 | HJ_Mod_Sort (매우 짧아서 판단 불가) |

---

## 🔴 카테고리 1: 오빠두 잔재 가능성 높음

이들은 **오빠두엑셀 코드를 그대로 복사**했고, **현재 H-ON 회계시스템에서 사용되지 않는 코드**입니다.

### Mod_WS.bas
- **추정 출처**: 오빠두엑셀 100%
- **확신도**: 높음 🔴
- **현황**: **전체 266줄이 `''`로 주석 처리**
- **오빠두 잔재**:
  - `Sub OpenfrmProduct()` — 제품폼 열기
  - `Sub OpenfrmInventorySearch()` — 재고 검색폼 열기
  - `Sub InventoryDate()` — 재고 입출고 날짜 입력
  - `Sub Insert_Inventory()` — 재고 입출고 기록
  - `Sub GetCurrentBalance()` — 현재 재고 잔액 조회
  - `Sub Delete_Inventory()` — 재고 정보 삭제
  - `Sub 전표입력()` — 직접 엑셀 입력 기반 전표입력 (이전 방식)
  - `Sub 전표인쇄()` — 레거시 전표인쇄
  - 주석: "재고정보 초기화", "재고 입출고 정보", "현재 보관중인 재고" 등
- **H-ON에서 추가/수정**: 없음 (전부 비활성화)
- **현재 사용 여부**: ❌ 사용 안 함
- **삭제 가능성**: **매우 높음** ✅
- **삭제 시 주의**: 없음 (호출 흔적 0)
- **판단 근거**:
  - 오빠두의 Mod_WS (133줄)를 완전히 복사한 후 주석 처리
  - 새로운 회계 시스템용 코드(frmAcctInput, frm반제전표 등)로 기능 대체됨
  - Git 이력에 있으므로 안전하게 삭제 가능

**→ 안전하게 삭제 가능한 코드 1순위**

---

### frmProduct.frm
- **추정 출처**: 오빠두엑셀 95%+
- **확신도**: 높음 🔴
- **코드 비교**:
  ```vba
  ' 오빠두 (274줄) vs H-ON (282줄) — 거의 동일
  ' 처음 65줄이 완벽하게 일치:
  - Dim UnLockTime As Date
  - Private Sub btnCategory_MouseDown(...) → frmCategory.Show
  - Private Sub btnCustomer_MouseDown(...) → frmCustomer.Show
  - Private Sub btnDelete/Edit/Register_MouseDown(...)
  - Private Sub btnUnit_MouseDown(...) → frmUnit.Show
  ```
- **오빠두 잔재**:
  - 제품/SKU 관리 로직 (Delete_Product, Edit_Product, Register_Product)
  - "카테고리", "단위", "고객사" 선택 구조 (H-ON에는 불필요)
  - `arrCustomer` 전역 배열 참조 (재고 시스템 패턴)
  - 리스트박스 검색 UI (txtSearch_KeyUp → Update_lstMain)
- **H-ON에서 추가/수정**: 
  - UserForm_Initialize에 3줄 추가 (82-84줄)
  - 그 외 실질적인 변경 없음
- **현재 사용 여부**: ❌ 호출 흔적 없음
- **삭제 가능성**: **매우 높음** ✅
- **삭제 시 주의**: 없음 (어디서도 호출되지 않음)
- **판단 근거**:
  - 오빠두 frmProduct (274줄)와 라인별 비교 시 95% 이상 동일
  - 8줄 증가는 미미한 초기화 코드만 추가됨
  - 재고관리 시스템의 전형적인 "마스터 데이터 관리" 폼 구조
  - 함수명: `Delete_Product`, `Edit_Product`, `Register_Product` (제품 중심)
  - H-ON 회계시스템에서는 사용하지 않는 기능

**→ 안전하게 삭제 가능한 코드 2순위**

---

### frmProductSelect.frm
- **추정 출처**: 오빠두엑셀 95%+
- **확신도**: 높음 🔴
- **코드 비교**:
  ```vba
  ' 오빠두 (81줄) vs H-ON (88줄) — 거의 동일
  ' 주요 함수: SelectProduct()
  ' 리스트박스 기반 제품 선택 폼
  ```
- **오빠두 잔재**:
  - 제품 검색 및 선택 기능
  - 리스트박스 더블클릭으로 선택 (lstMain_DblClick)
  - 검색 텍스트박스 (txtSearch_KeyUp)
  - OnHover_Css / OutHover_Css 스타일 함수 (마우스 호버 효과)
- **H-ON에서 추가/수정**: 
  - 7줄만 증가 (변경 없음)
- **현재 사용 여부**: ❌ 호출 흔적 없음
- **삭제 가능성**: **매우 높음** ✅
- **삭제 시 주의**: 없음 (비활성 폼)
- **판단 근거**:
  - 오빠두 frmProductSelect (81줄)와 거의 동일
  - 제품 선택 팝업 (H-ON 회계시스템에는 불필요)
  - 재고 시스템의 보조 폼

**→ 안전하게 삭제 가능한 코드 3순위**

---

## 🟠 카테고리 2: 오빠두 기반 + H-ON 수정

이들은 **오빠두엑셀의 코드를 기반으로 하되**, H-ON 회계시스템을 위해 **수정/확장한 코드**입니다. 대부분 **유틸리티 모듈**이고, 기본 골격은 오빠두이지만 H-ON에서도 활발히 사용 중입니다.

### z_Mod_DB.bas
- **추정 출처**: 오빠두 60% + H-ON 40%
- **확신도**: 높음 🟠
- **코드 비교**:
  ```vba
  ' 오빠두 (537줄) vs H-ON (669줄) — 132줄 증가
  ' 공통 부분 (처음 ~400줄): 거의 동일
  - Get_MaxID(ws) — 최대 ID 반환
  - Get_InsertRow(ws) — 삽입할 행 번호
  - Get_ColumnCnt(ws) — 열 개수
  - get_UpdateRow(ws, ID) — 업데이트 행 번호
  - Get_DB(ws) — 배열로 DB 반환
  - Get_Records(ws, ID, Fields) — 특정 레코드 조회
  ```
- **오빠두에서 가져온 부분 (배열 기반 워크시트 DB)**:
  - 워크시트를 DB처럼 사용하는 유틸함수들
  - 주석 스타일: "이번 예제파일에서만 사용" (오빠두 원본 주석)
  - 배열 조작 중심 로직
- **H-ON에서 추가한 부분 (130줄+)**:
  - `Get_MaxPrintNo(ws)` — **전표번호 생성** (회계 전용)
  - ACCESS DB용 추가 함수들 가능성
  - 회계전표 관련 데이터 처리
- **현재 사용 여부**: ✅ 활발히 사용 (배열 기반 데이터 처리)
- **삭제 가능성**: 매우 낮음 ❌ (여전히 필요)
- **삭제 시 주의**: 불가능 (현재 시스템 운영 중)
- **판단 근거**:
  - 오빠두 z_Mod_DB (537줄)를 기반으로 수정
  - "Get_" 패턴 함수들은 오빠두 특징
  - Get_MaxPrintNo() 추가로 회계 기능 확장
  - 여전히 워크시트 기반 데이터 처리에 사용

**→ 유지 필요, 필요시 나중에 리팩토링**

---

### z_Mod_Array.bas
- **추정 출처**: 오빠두 95% + H-ON 5%
- **확신도**: 높음 🟠
- **코드 비교**:
  ```vba
  ' 오빠두 (302줄) vs H-ON (304줄) — 2줄만 증가
  ' 거의 동일
  ```
- **오빠두 코드**:
  - 배열 조작 유틸함수
  - `ArrayToRng()`, `RngToArray()`, `SortArray()` 등
  - 배열 필터링, 변환 기능
- **H-ON에서 추가**: 미미 (거의 없음)
- **현재 사용 여부**: ✅ 활발히 사용 (배열 처리)
- **삭제 가능성**: 낮음 ❌ (여전히 필요)
- **판단 근거**:
  - 오빠두 z_Mod_Array를 그대로 복사
  - 2줄만 변경 (거의 변경 없음)
  - 배열 기반 데이터 처리에 필수

**→ 유지 필요**

---

### z_Mod_Frm.bas
- **추정 출처**: 오빠두 40% + H-ON 60%
- **확신도**: 높음 🟠
- **코드 구조**:
  ```vba
  ' 오빠두 (209줄) vs H-ON (512줄) — 303줄 대폭 증가
  
  ' 오빠두 기본 부분 (1-200줄):
  - Update_Cbo() — 콤보박스 DB 갱신
  - Select_CboItm() — 콤보박스 아이템 선택
  - Update_List() — 리스트박스 갱신
  - Get_ListItm(), Select_ListItm() — 리스트박스 아이템 조작
  - Active_ListBox(), Get_ListIndex(), isListBoxSelected() — 상태 확인
  
  ' H-ON 추가 부분 (200-512줄):
  - Update계정코드검색() — [혜정] 회계 계정 검색
  - Update은행코드검색() — 은행 코드 검색
  - 차대합계_일치_Public() — 전표입력 화면의 차/대변 합계 검증
  - 반제전표() — 선금/외상 반제 처리
  - 반제_업데이트() — 반제 정보 업데이트
  - 반제_업데이트_DB() — DB 반제 업데이트
  - ToggleControl() — 컨트롤 활성/비활성 제어
  - Update_cbo계정코드() — 계정코드 콤보박스 갱신
  - ShowStatus() — 상태 표시 (현재 미호출)
  ```
- **오빠두 코드**:
  - 폼 컨트롤 기본 유틸함수들
  - 배열 기반 콤보박스/리스트박스 처리
  - "Update_", "Get_", "Select_", "Active_" 패턴
- **H-ON에서 추가한 부분**:
  - 회계 시스템 특화 함수들 (계정코드, 은행코드 검색)
  - 반제전표 처리 로직 (선금/외상 회수)
  - 차/대변 합계 검증
  - 주석: "[혜정]" (개발자 이름) 표시
- **현재 사용 여부**: ✅ 매우 활발히 사용 (35회 호출)
- **삭제 가능성**: 불가능 ❌
- **판단 근거**:
  - 오빠두 기본 구조 유지 + H-ON 대규모 확장
  - 라인수 2.4배 증가 (209→512줄)
  - "혜정" 주석으로 구분된 신규 코드
  - 회계시스템의 핵심 폼 유틸 모듈

**→ H-ON에서 대규모 확장한 모듈, 유지 필수**

---

### frmCalendar.frm
- **추정 출처**: 오빠두 98% + H-ON 2%
- **확신도**: 높음 🟠
- **코드 비교**:
  ```vba
  ' 오빠두 (374줄) vs H-ON (379줄) — 5줄만 증가
  ' 거의 동일한 캘린더 UI
  ```
- **오빠두 코드**:
  - 날짜 선택 캘린더 폼
  - 월/년도 스크롤, 버튼 클릭 처리
  - 마우스 호버 효과 (OnHover_Css)
  - 공통 함수 호출 (z_Module_GetCursor 등)
- **H-ON에서 추가**: 미미 (5줄)
- **현재 사용 여부**: ✅ 여러 곳에서 사용 (전표 입력 등)
- **삭제 가능성**: 불가능 ❌ (필수 유틸리티)
- **판단 근거**:
  - 오빠두 frmCalendar를 거의 그대로 복사
  - 변경 최소화 (5줄만 증가)
  - 날짜 선택 기능은 회계시스템에서도 필수

**→ 오빠두 코드를 최소한으로 유지한 모듈**

---

### z_Mod_Sort.bas
- **추정 출처**: 오빠두 100%
- **확신도**: 높음 🟠
- **코드 비교**:
  ```vba
  ' 오빠두 (174줄) vs H-ON (174줄) — 완전 동일
  ```
- **오빠두 코드**: 정렬 유틸함수
- **변경**: 0줄 (완전 동일)
- **현재 사용 여부**: ✅ 배열 정렬에 사용
- **판단 근거**: 오빠두 코드를 그대로 복사, 수정 없음

---

### z_Module_GetCursor.bas
- **추정 출처**: 오빠두 100%
- **확신도**: 높음 🟠
- **코드**: 마우스 커서 위치 감지 유틸
- **변경**: 0줄 (완전 동일)

---

### z_Mod_Shape.bas
- **추정 출처**: 오빠두 100%
- **확신도**: 높음 🟠
- **코드**: 도형 처리 유틸
- **변경**: 0줄 (완전 동일)

---

### z_Module_ListWheel.bas
- **추정 출처**: 오빠두 100%
- **확신도**: 높음 🟠
- **코드**: 마우스휠로 리스트박스 스크롤
- **변경**: 0줄 (완전 동일)

---

## 🟢 카테고리 3: H-ON 자체 코드 (24개 모듈)

이들은 H-ON 회계시스템을 위해 **새로 작성한 코드**입니다. 회계, DB, 거래처 관리 등 회계 도메인 특화 기능입니다.

### 회계 보고서 생성 (5개 모듈)
- **HJ_Mod_Report.bas** (1,039줄) — 합계잔액시산표 생성 (워크시트 기반)
- **Mod_ACCESS_Report.bas** (919줄) — 합계잔액시산표 생성 (DB 기반)
- **Mod_ACCESS_Report_BS.bas** (598줄) — 재무상태표(대차대조표) 생성
- **Mod_ACCESS_Report_PL.bas** (563줄) — 손익계산서 생성
- **HJ_Mod_NEOS_Main.bas** (162줄) — PDF 내보내기

**특징**:
- 회계 용어 (차변, 대변, 원장, 계정 등)
- 한글 함수명 (재무상태표_생성, 손익계산서_생성)
- ACCESS DB 기반 데이터 처리
- 오빠두 재고시스템에는 없는 기능

### 회계 데이터 처리 (5개 모듈)
- **Mod_ACCESS.bas** (99줄) — DB 연결
- **Mod_ACCESS_DB.bas** (1,171줄) — 전표, 거래처, 계정 관리
- **Mod_ACCESS_Closing.bas** (487줄) — 결산 마감 처리
- **HJ_Mod_Sort.bas** (64줄) — 계정 정렬
- **Mod_ACCESS_Report.bas** (919줄) — 보고서 DB 조회

### 회계 입력 폼 (10개 폼)
- **frmAcctInput.frm** (638줄) — 회계전표 입력 (메인)
- **frm반제전표.frm** (594줄) — 선금/외상 반제
- **frm전표인쇄.frm** (562줄) — 전표 인쇄
- **frm외화평가.frm** (360줄) — 외화 평가 전표
- **frm채권채무조회.frm** (356줄) — 외상/선금 현황
- **frm거래처관리.frm** (347줄) — 거래처 정보 관리
- **frm제예금명세.frm** (196줄) — 제예금 현황
- **frm원장조회.frm** (150줄) — 총계정원장 조회
- **frm송금리스트.frm** (171줄) — 송금 예정 리스트
- **frm결산마감.frm** (1줄) — 결산 마감 (미완성)

### 거래처/계정 관리 (3개 폼)
- **frm거래처검색.frm** (68줄) — 거래처 검색
- **frm계정검색.frm** (65줄) — 계정 검색
- **frmMain.frm** (269줄) — 메인 대시보드/공지사항

### 기타 (1개 폼)
- **frm로그인.frm** (75줄) — 로그인
- **frm즐겨찾기.frm** (107줄) — 자주 쓰는 전표 템플릿
- **frm보고서조회.frm** (89줄) — 보고서 조회

**특징**:
- 모든 함수명/변수명이 회계 도메인 중심
- ACCESS DB 기반 (배열 기반 아님)
- 전표, 거래처, 계정, 계정과목 등 회계 용어
- 오빠두 재고시스템에는 전혀 없는 기능

---

## ⚪ 카테고리 4: 출처 판단 불가

### HJ_Mod_Sort.bas (64줄)
- **특징**: 매우 짧고 public 함수 없음
- **판단 불가 이유**: 코드 내용으로는 "Sort"라는 이름만 있고 실제 로직 파악 어려움
- **추정**: 회계보고서의 정렬 로직일 가능성 높음 (HJ_ 접두사)

---

## 📈 분석 결과 통계

```
┌─────────────────────────────────────────────┐
│ H-ON 회계시스템 VBA 모듈 출처 분석 결과    │
├─────────────────────────────────────────────┤
│ 🔴 오빠두 잔재 가능성 높음: 3개 (8%)       │
│    - Mod_WS.bas                          │
│    - frmProduct.frm                      │
│    - frmProductSelect.frm                │
│                                          │
│ 🟠 오빠두 기반 + H-ON 수정: 8개 (22%)      │
│    - z_Mod_DB.bas (+132줄 추가)          │
│    - z_Mod_Frm.bas (+303줄 확장)         │
│    - z_Mod_Array.bas (+2줄)              │
│    - frmCalendar.frm (+5줄)              │
│    - z_Mod_Sort.bas (동일)               │
│    - z_Module_GetCursor.bas (동일)       │
│    - z_Mod_Shape.bas (동일)              │
│    - z_Module_ListWheel.bas (동일)       │
│                                          │
│ 🟢 H-ON 자체 코드: 24개 (67%)              │
│    - 회계보고서 5개                       │
│    - 회계DB/처리 5개                      │
│    - 회계 입출력 폼 10개                  │
│    - 메인/거래처/계정 관리 4개            │
│                                          │
│ ⚪ 출처 판단 불가: 1개 (3%)               │
│    - HJ_Mod_Sort.bas                     │
└─────────────────────────────────────────────┘
```

---

## 🎯 실무 가이드

### 즉시 삭제 가능한 코드 (위험도 매우 낮음)
```
✅ Mod_WS.bas — 전체 주석 처리, 사용 안 함
✅ frmProduct.frm — 재고 폼, 호출 없음
✅ frmProductSelect.frm — 재고 보조 폼, 호출 없음
```

**이들을 삭제해도**:
- 회계시스템 기능에 영향 없음
- 호출 흔적 0
- Git 이력에 남아있음

### 유지해야 할 코드
```
⚠️ z_Mod_* (배열/DB/폼/정렬 유틸) — 여전히 필수
⚠️ frmCalendar, z_Module_* — 공통 유틸

🔴 회계 시스템 코드 (Mod_ACCESS*, frm*, HJ_*) — 필수
```

### 향후 리팩토링 방향
1. **1단계**: 재고 잔재 3개 모듈 삭제 (위험도 낮음)
2. **2단계**: z_Mod_DB.bas 분석
   - 배열 기반 vs DB 기반 사용처 구분
   - ACCESS DB 전환 여부에 따라 결정
3. **3단계**: z_Mod_Frm.bas 정리
   - 오빠두 기본 함수는 유지
   - H-ON 추가 함수는 분리 검토
4. **4단계**: 회계 모듈들 구조화
   - Mod_ACCESS_DB.bas 분할 (테이블별)
   - 보고서 모듈 통합 검토 (HJ_Mod_Report vs Mod_ACCESS_Report)

---

## 결론

H-ON 회계시스템은 **오빠두엑셀 재고관리 시스템의 기본 구조(유틸 모듈, 폼 컨트롤 처리)를 재사용**하면서, **회계 도메인의 새로운 모듈들을 대규모로 추가**했습니다.

**구성**:
- 📚 **기초**: 오빠두 유틸 (~8개 모듈, 100% 유지)
- 🔧 **중간**: 오빠두 기반 회계용 확장 (~1개 모듈, 대폭 수정)
- 🏛️ **상위**: H-ON 회계시스템 신규 (~24개 모듈, 100% 신규)
- 🗑️ **유산**: 오빠두 재고 관련 비활성 코드 (~3개 모듈, 즉시 삭제 가능)

> **중요**: 이 분석은 **코드 수정이 아닌 분석만 수행**했습니다. 
> 실제 삭제/리팩토링은 이 결과를 검토한 후 별도 단계에서 진행해주세요.

